version: '3.8'

services:

  # ═══════════════════════════════════════
  # DATABASES
  # ═══════════════════════════════════════

  mysql-user:
    image: mysql:8.0
    container_name: mysql-user
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: user_service_db
    ports:
      - "3307:3306"
    volumes:
      - mysql_user_data:/var/lib/mysql
      - ./init-db/user_service.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - microservice-net

  mysql-order:
    image: mysql:8.0
    container_name: mysql-order
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: order_service_db
    ports:
      - "3308:3306"
    volumes:
      - mysql_order_data:/var/lib/mysql
      - ./init-db/order_service.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - microservice-net

  # ═══════════════════════════════════════
  # MOCK SERVICES (bạn kia làm sau)
  # ═══════════════════════════════════════

  mock-catalog-service:
    build: ./mock-catalog-service
    container_name: mock-catalog-service
    restart: unless-stopped
    environment:
      PORT: 3002
    ports:
      - "3002:3002"
    networks:
      - microservice-net

  mock-trip-service:
    build: ./mock-trip-service
    container_name: mock-trip-service
    restart: unless-stopped
    environment:
      PORT: 3003
    ports:
      - "3003:3003"
    networks:
      - microservice-net

  # ═══════════════════════════════════════
  # USER SERVICE
  # ═══════════════════════════════════════

  user-service:
    build: ./user-service
    container_name: user-service
    restart: unless-stopped
    environment:
      PORT: 3001
      DB_HOST: mysql-user
      DB_PORT: 3306
      DB_NAME: user_service_db
      DB_USER: root
      DB_PASS: rootpassword
      ACCESS_TOKEN_SECRET: my_super_secret_jwt_key_2024
      ALLOWED_ORIGIN: http://localhost:5173
    ports:
      - "3001:3001"
    depends_on:
      mysql-user:
        condition: service_healthy
    networks:
      - microservice-net

  # ═══════════════════════════════════════
  # NOTIFICATION SERVICE
  # ═══════════════════════════════════════

  notification-service:
    build: ./notification-service
    container_name: notification-service
    restart: unless-stopped
    environment:
      PORT: 3005
      # --- Điền vào đây để gửi email thật ---
      MAIL_USER: $MAIL_USER   # your_gmail@gmail.com
      MAIL_PASS: $MAIL_PASS   # App Password từ Google
    ports:
      - "3005:3005"
    networks:
      - microservice-net

  # ═══════════════════════════════════════
  # ORDER SERVICE
  # ═══════════════════════════════════════

  order-service:
    build: ./order-service
    container_name: order-service
    restart: unless-stopped
    environment:
      PORT: 3004
      DB_HOST: mysql-order
      DB_PORT: 3306
      DB_NAME: order_service_db
      DB_USER: root
      DB_PASS: rootpassword
      TRIP_SERVICE_URL: http://mock-trip-service:3003
      CATALOG_SERVICE_URL: http://mock-catalog-service:3002
      NOTIFICATION_SERVICE_URL: http://notification-service:3005
      ALLOWED_ORIGIN: http://localhost:5173
      FRONTEND_URL: http://localhost:5173
      # --- VNPAY Sandbox (đăng ký tại sandbox.vnpayment.vn) ---
      VNPAY_TMN_CODE: $VNPAY_TMN_CODE
      VNPAY_HASH_SECRET: $VNPAY_HASH_SECRET
      VNPAY_RETURN_URL: $VNPAY_RETURN_URL
    ports:
      - "3004:3004"
    depends_on:
      mysql-order:
        condition: service_healthy
      mock-trip-service:
        condition: service_started
      mock-catalog-service:
        condition: service_started
      notification-service:
        condition: service_started
    networks:
      - microservice-net

  # ═══════════════════════════════════════
  # API GATEWAY
  # ═══════════════════════════════════════

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    restart: unless-stopped
    environment:
      PORT: 3000
      ACCESS_TOKEN_SECRET: my_super_secret_jwt_key_2024
      USER_SERVICE_URL: http://user-service:3001
      ORDER_SERVICE_URL: http://order-service:3004
      CATALOG_SERVICE_URL: http://mock-catalog-service:3002
      TRIP_SERVICE_URL: http://mock-trip-service:3003
      FRONTEND_URL: http://localhost:5173
    ports:
      - "3000:3000"
    depends_on:
      - user-service
      - order-service
      - mock-catalog-service
      - mock-trip-service
    networks:
      - microservice-net

volumes:
  mysql_user_data:
  mysql_order_data:

networks:
  microservice-net:
    driver: bridge
